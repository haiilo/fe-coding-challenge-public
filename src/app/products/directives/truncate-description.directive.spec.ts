import { TruncateDescriptionDirective } from './truncate-description.directive';

describe('TruncateDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncateDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
